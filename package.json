{
  "name": "voting-app",
  "version": "1.0.0",
  "description": "A voting pipeline built with the serverless framework.",
  "scripts": {
    "deploy": "cd voting-functions && serverless deploy",
    "remove": "cd voting-functions && serverless remove",
    "deploy:db": "cd voting-db && serverless deploy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.437.0",
    "serverless": "^1.40.0"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "eslint": "^5.16.0",
    "mocha": "^6.1.4"
  }
}
